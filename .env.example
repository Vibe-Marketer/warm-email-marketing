# Warm Email Marketing - Environment Variables
# VERSION: 2026.01.09
#
# ⚠️ REQUIRED: Copy this file to .env and replace ALL placeholder values
# ⚠️ Docker will FAIL if you don't replace the CLOUDFLARE_TUNNEL_TOKEN
#
# Instructions: See SETUP.md or IMPLEMENTATION_PLAN.md

# =============================================================================
# CLOUDFLARE TUNNEL (REQUIRED - System will not start without this)
# =============================================================================
# STEP 1: Create tunnel at https://one.dash.cloudflare.com/ → Networks → Tunnels
# STEP 2: Copy the tunnel token (starts with eyJ...)
# STEP 3: Paste it below, replacing REPLACE_WITH_YOUR_TUNNEL_TOKEN
#
# ⚠️ This MUST be replaced or Docker containers will fail to start
CLOUDFLARE_TUNNEL_TOKEN=REPLACE_WITH_YOUR_TUNNEL_TOKEN

# =============================================================================
# OPTIONAL: Additional Cloudflare settings (not required for basic setup)
# =============================================================================
# Only needed if you want to use Cloudflare API for DNS automation
CLOUDFLARE_API_TOKEN=optional_cloudflare_api_token_here
CLOUDFLARE_ZONE_ID=optional_zone_id_here
CLOUDFLARE_BASE_DOMAIN=yourdomain.com
CLOUDFLARE_ACCOUNT_ID=optional_account_id_here

# =============================================================================
# RESEND (REQUIRED for sending emails)
# =============================================================================
# You'll configure this in Listmonk UI after setup, not here
# But you can store your API key here for reference
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_REPLACE_WITH_YOUR_RESEND_API_KEY

# =============================================================================
# DATABASE (Leave these as default or customize)
# =============================================================================
POSTGRES_USER=listmonk
POSTGRES_PASSWORD=listmonk_secure_password_2026
POSTGRES_DB=listmonk
